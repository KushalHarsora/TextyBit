import React from "react";

const Page = () => {
  return(
    <React.Fragment>
      <main className=" h-screen w-screen flex justify-center items-center">
        Texty Bit
      </main>
    </React.Fragment>
  );
}

export default Page;